package src.models.mobility;

public class Car extends Vehicle{

    public Car(String color, String registrationNo, VehicleType vehicleType) {
        super(color, registrationNo, vehicleType);
    }
}
